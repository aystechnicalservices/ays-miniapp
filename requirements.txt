fastapi==0.95.2
pydantic==1.10.26
uvicorn==0.30.6
python-telegram-bot==21.6
# httpx already comes in transitively via python-telegram-bot; pinned here
# explicitly too since app/gemini.py now depends on it directly as well.
httpx==0.28.1
python-dotenv==1.0.1
python-multipart==0.0.9
tzdata==2024.1
